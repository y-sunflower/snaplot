def add_digit(a, b):
    """
    Add 2 numbers (`a` and `b`)

    Parameters
    ---

    - `a`: a number
    - `b`: a number

    Return
    ---

    The sum between `a` and `b`


    Example
    ---

    ```py
    from snaplot import add_digit

    add_digit(5, 8)
    ```
    """
    return a + b
